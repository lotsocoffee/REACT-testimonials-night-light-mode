import ReactDOM  from "react-dom/client";
import "./index.css";
import App from "./App";  //el primer App es el nombre del componente App de la funcion en App.js

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
    <App></App>
);